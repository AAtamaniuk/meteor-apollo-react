import React from 'react';
import PropTypes from 'prop-types';

const App = (props) => {
    return (
        <h1>Hello!</h1>
    );
};

App.propTypes = {};
App.defaultProps = {};

export default App;
